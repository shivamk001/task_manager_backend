import jwt from 'jsonwebtoken';

export class AuthService{

    public static async registrationService(email: string, userName: string, password: string){
    }

    public static async loginService(email: string, password: string){
    }
}